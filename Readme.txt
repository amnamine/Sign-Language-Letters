## Requirements

To run this project, you need to have the following libraries installed:

- Python (version X.X.X)
- OpenCV (version X.X.X)
- Mediapipe (version X.X.X)

what is this model : hand tracking module with angle detection for signes that i created called thrat sender to police and am still working on it
